# animal-faces-classification
Dataset was retirieved from kaggle. Multi-class Classification to be implemented for image classification within pytorch. 

## Setup

### Create environment
```bash
conda env create -f environment.yml
conda activate animal-faces-classification
```

### Install dependencies
`pip install -r requirements.txt`
